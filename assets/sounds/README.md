# Rest timer notification sound

Rest completion uses the **device OS default notification sound** via
`expo-notifications` (`sound: "default"` in `notificationService`).

No custom in-app audio file is required.
